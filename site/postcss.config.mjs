/** Tailwind v4 ships its PostCSS integration as a separate package; this
 *  file is the whole configuration, the theme lives in app/globals.css. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
