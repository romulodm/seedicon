{
  "name": "seedicon-site",
  "private": true,
  "version": "0.0.0",
  "description": "Marketing site + playground for the seedicon package. Never published to npm.",
  "scripts": {
    "prebuild": "cd .. && npm ci --include=dev --no-audit --no-fund && npm run build",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "seedicon": "file:.."
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.13",
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tailwindcss": "^4.1.13",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "postcss": "^8.5.28"
  }
}
